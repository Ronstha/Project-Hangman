#include <stdio.h>

/*function StartGame(difficulty){
//while True:
    //words=getWords()  
    current=words[randomWord(words)]
    cscore=game(current)
    score+=cscore
    if cscore==0:
        GameOver()



}

function getWords(int difficulty){
    //datfile:easy 0, medium 1, hard 2
    words=[words+hint from dat file]
    return words
}
function randomWord(words){
    return randomIndex
}
function game(current){
//Game_Screen_Content
//misatakes=0
while true:
    letter:: get letter from user
    if letter in current:
        update letter in _
        if all guessed 
            return score??
    else:
        mistakes+=1
        update Hangman according to mistakecount 
        if mistakecount=6:
          return 0
        

    update characters
       
     

  

}
function GameOver(){
    //Display Score CorrectWord 
    CheckHighscore()
}

function checkHighscore(){
    score higher than or equal to top10
        ask name()
        store score()
}

/*